# Local working copies — not needed by the site.
# The two .mp4s the site actually loads live at the repo root; Videos/ is a
# byte-identical duplicate kept locally for the asset library.
Videos/
.claude/

# Print-resolution source files. They live in the asset library outside this
# repo, and must never be served by the public site again.
Images/
